# Copie este arquivo para .env e preencha os valores.
# cp .env.example .env
#
# O .env é carregado em development e test (não em production).

# Banco de dados (opcional em dev se usar docker-compose)
# DATABASE_URL=postgres://challenge:challenge@localhost:5432/challenge_development

# Redis (opcional em dev se usar docker-compose)
# REDIS_URL=redis://localhost:6379/0

# --- S3 (importação de CSV) ---
# Se S3_IMPORT_BUCKET não estiver definido, os arquivos ficam em storage/ local.
# S3_IMPORT_BUCKET=seu-bucket-imports
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
